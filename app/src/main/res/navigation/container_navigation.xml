<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container_navigation"
    app:startDestination="@+id/navHome">

    <fragment
        android:id="@+id/navHome"
        android:label="@string/home"
        android:name="com.experiment.hts.storedecor.view.fragment.HomeFragment"
        tools:layout="@layout/fragment_home"/>

    <fragment
        android:id="@+id/navUser"
        android:label="@string/user"
        android:name="com.experiment.hts.storedecor.view.fragment.UserFragment"
        tools:layout="@layout/fragment_user"/>

    <fragment
        android:id="@+id/navCategory"
        android:label="@string/category"
        android:name="com.experiment.hts.storedecor.view.fragment.CategoryFragment"
        tools:layout="@layout/fragment_category"/>

    <fragment
        android:id="@+id/navNotification"
        android:label="@string/notification"
        android:name="com.experiment.hts.storedecor.view.fragment.NotificationFragment"
        tools:layout="@layout/fragment_notification"/>

</navigation>